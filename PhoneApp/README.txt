Installing/running Cordova PhoneGap on MacOS X

$ sudo npm install -g phonegap
$ sudo npm install -g ios-sim
$ phonegap create myApp
$ cd myApp
$ phonegap run

After that the iOS simulator is opened with the app installed/running.

Ps: XCode needed, for sure!